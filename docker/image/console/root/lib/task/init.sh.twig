#!/bin/bash

function task_init()
{
    task "mysql:available"
    task "assets:apply"

    (
        cd /app/{{ @('drupal.docroot') }}
        {% for step in @('backend.init.steps') -%}
        {{ step|raw }}
        {% endfor %}
        
    )

    task "welcome"
}
